<Alloy>
   <Window class="container">
   	<Button id="allowPush" onClick="allowPush">Allow Push Notifications?</Button>
   	<Button id="facebookConnect" onClick="facebookConnect">Connect to FaceBook?</Button>
   	</Window>
</Alloy>